import * as db from "./databaseManager";
import * as respond from "./response";
import * as handle from "./dataHandler";
import * as ws from "./websocketManager";

export default {
    db,
    ws,
    respond,
    handle,
};
